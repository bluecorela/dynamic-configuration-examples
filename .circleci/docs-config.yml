version: 2.1

parameters:
  always-continue:
    type: boolean
    default: false
    
jobs:
  build-docs:
    docker:
      - image: cimg/base:stable
    steps:
      - run: echo "Building docs"

workflows:
  s-workflow:
    jobs:
      - lint # use the shared lint job.
      - build-docs